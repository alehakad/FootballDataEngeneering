config {
  type: "table",
  schema: "players_prices",
  name: "resolved_teams"
}

SELECT
   tm.id as team_id,
   team_name_fbref as team_name
FROM ${ref("club_data")} cd
JOIN
  ${ref("team_name_mapping")} tm
ON
  CAST(cd.id AS INT) = tm.id