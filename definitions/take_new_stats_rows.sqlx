-- taking only new rows from staging_stats_table --

config {
  type: "table",
  schema: "players_stats",
  name: "new_stats_rows"
}

SELECT
    *
FROM ${ref("staging_stats_table")}
WHERE ingestion_timestamp > (SELECT MAX(ingestion_timestamp) FROM ${self()})


