-- config {
--   type: "table",
--   schema: "intermediate",
--   name: "tomer_test"
-- }

-- SELECT
--    pd.player_id,
--    sr.*,
--    pd.team_id
-- FROM ${ref("clean_new_stats_rows")} sr
-- LEFT JOIN ${ref("clean_price_data")} pd
-- ON sr.team_ = pd.team_name
-- AND ABS(DATE_DIFF(sr.birth_date, pd.birth_date, DAY)) <= 2